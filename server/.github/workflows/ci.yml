name: Backend CI

on:
  push:
    branches: [ main ]

jobs:
  build-and-check:
    runs-on: ubuntu-latest

    steps:
      - name: ⬇ Checkout repository
        uses: actions/checkout@v3

      - name:  Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name:  Install dependencies
        run: npm install
        working-directory: ./server

      - name: Check server starts (basic build step)
        run: echo "No build needed, just a test placeholder."
